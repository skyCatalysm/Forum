<nav class="navbar">
    <div class="container">
        <div class="navbar-brand">
            <a class="navbar-item forum-nav-font has-text-weight-semibold" href="{{ path('forum') }}">
                FORUM
            </a>
            <div class="navbar-burger burger is-active" data-target="navbarExampleTransparentExample">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>

        <div id="navbarExampleTransparentExample" class="navbar-menu is-active">
            <div class="navbar-end">
                <a class="navbar-item forum-nav-font" href="{{ path('sec_login') }}">Sign In</a>
                <a class="navbar-item forum-nav-font" href="{{ path('sec_register') }}">Register</a>
            </div>
        </div>
    </div>
</nav>